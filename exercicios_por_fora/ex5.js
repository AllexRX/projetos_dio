let salarioBrutoUser = 3000;
let salarioMinimo = 1212;
let quantidadeSalariosMinimos = salarioBrutoUser / salarioMinimo;
console.log(`Quem recebe um salário de R$${salarioBrutoUser.toFixed(2)} ganha aproximadamente ${quantidadeSalariosMinimos.toFixed(2)} salários mínimos.`);